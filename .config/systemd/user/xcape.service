[Unit]
Description=XCape Daemon
After=graphical.target

[Service]
Type=forking
Environment=DISPLAY=:0
ExecStart=/run/current-system/sw/bin/xcape -e "Hyper_L=slash;Hyper_R=backslash"
Restart=always
RestartSec=1

[Install]
WantedBy=default.target
